public class TechSupport extends Employee {
    private static double maxSalary = 3500;

    public TechSupport(String firstName, String lastName, String pesel) {
        super(firstName, lastName, pesel);
    }
}
